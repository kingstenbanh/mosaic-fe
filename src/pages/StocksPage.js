import React from 'react'

const StocksPage = ({ data }) => {
  const { symbol, earnings } = data;
  
  return (
    <div className="StocksPage">
      StockPages
    </div>
  )
};

export default StocksPage;